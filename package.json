{
  "name": "ai-hr-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@livekit/components-react": "^1.2.0",
    "@livekit/client": "^1.2.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.17.0",
    "react-scripts": "^5.0.1",  // Убедись, что эта зависимость есть
    "web-vitals": "^3.0.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",  // добавлена зависимость для очистки кэша
    "@babel/plugin-proposal-private-property-in-object": "^7.18.6"
  },
  "scripts": {
    "start": "react-scripts start",  // команда для старта
    "build": "react-scripts build",  // команда для сборки
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean": "rimraf node_modules/.cache"  // команда для очистки кэша
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
